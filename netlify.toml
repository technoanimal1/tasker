# Netlify build config for the Vite SPA.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback (harmless; app is single-page).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
